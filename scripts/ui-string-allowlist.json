{
  "AgentPermissionsPanel.tsx": [
    "text:() const [saving, setSaving] = createSignal",
    "text:(new Set()) const [errors, setErrors] = createSignal",
    "text:const KNOWN_CATEGORIES = ['edit', 'execute', 'read', 'assets', 'settings', 'queue'] as const const CATEGORY_LABELS: Readonly",
    "text:principal.self && !principal.local) const refreshDelegations = async (): Promise"
  ],
  "App.tsx": [
    "attribute:aria-label:Execution provenance",
    "text:'close' path), and closing restores focus to the opener. A body that must swallow keys - e.g. the settings keybinding capture - calls preventDefault on the keydown, which suppresses the native close request. The host owns the chrome (::backdrop, title, close button); a keyed Show remounts the body per panel, so opening IS the reset. aria-modal is redundant on a modal",
    "text:('.product-tablist') const body = element.querySelector",
    "text:('.product-tabpanel:not([hidden])') const overflowButton = element.querySelector",
    "text:() const [failed, setFailed] = createSolidSignal",
    "text:() const layers = new Set",
    "text:()) setLoaded(new Set",
    "text:(new Set()) const [loaded, setLoaded] = createSolidSignal",
    "text:.canvas-pane') const canvas = center?.querySelector",
    "text:.editor-split-region') if (!center || !canvas) return undefined const state = app.dock.effective() const visibleZones: Partial",
    "text:0 let bodyElement!: HTMLDivElement const [panelDrag, setPanelDrag] = createSolidSignal",
    "text:1 // Pending close is validated by (id, DocumentSession): a same-id // replacement (import or collaboration adoption) installs a new session, // invalidating the user's discard decision, while editor-kind switches // replace the Tab object but keep the session and must not dismiss it. const [pendingClose, setPendingClose] = createSolidSignal",
    "text:= rect.left - pad && x",
    "text:= rect.top - pad && y",
    "text:Array.from(element.querySelectorAll",
    "text:Backend",
    "text:Backend ID",
    "text:Dinkster",
    "text:Execution",
    "text:Execution owner removed or disconnected. Retained output routes are used when available.",
    "text:Image unavailable",
    "text:No execution selected",
    "text:Nothing queued for this workflow.",
    "text:Run this workflow or open a recorded execution to inspect its outputs.",
    "text:Status",
    "text:app.panels.get(drag().panelId) return (",
    "text:app.settings.get",
    "text:candidate === host.dataset['zone']) const hostRect = host.getBoundingClientRect() if (zone === undefined || hostRect.width === 0 || hostRect.height === 0) continue const sections: PanelDragSectionGeometry[] = [] for (const element of host.querySelectorAll",
    "text:element.isConnected) target?.querySelector",
    "text:endRegionResize?.()) const [logsClearArmed, setLogsClearArmed] = createSolidSignal(false) let logsClearTimer: ReturnType",
    "text:entry.kind === 'workflow' ? [entry.workflowId] : [])) const detachedPanelIds = (): ReadonlySet",
    "text:liveTabOf(p.exec) return",
    "text:readonly ReadonlySignal",
    "text:regionObserver?.disconnect()) /** Uncommitted divider ratio while a resize drag is in flight. */ const [dividerPreview, setDividerPreview] = createSolidSignal",
    "text:shellEl.querySelector"
  ],
  "AppPreviewSurface.tsx": [
    "attribute:aria-label:Image preview",
    "text:Preview appears after execution.",
    "text:| undefined let lastFrameAnnouncementAt = 0 let revision = 0 const [announcement, setAnnouncement] = createSignal('') const [videoDims, setVideoDims] = createSignal"
  ],
  "AppView.tsx": [
    "text:!frozen() && !row.linked && !row.derived && row.spec !== undefined && editableKinds.has(row.spec.widgetType) const readOnlyHint = (row: Extract",
    "text:!frozen() && activeTab()?.appArrange === true const [containerWidth, setContainerWidth] = createSolidSignal(Number.POSITIVE_INFINITY) const [arrangeBreakpoint, setArrangeBreakpoint] = createSolidSignal",
    "text:('desktop') const [pendingArrangeBreakpoint, setPendingArrangeBreakpoint] = createSolidSignal",
    "text:() const mobileItemsByKey = createMemo((): ReadonlyMap",
    "text:() const placedParams = new Set",
    "text:() const placedPreviews = new Set",
    "text:() const remoteAborts = new Map",
    "text:(new Map()) const [pendingQueues, setPendingQueues] = createSolidSignal",
    "text:(new Map()) const remoteTokens = new Map",
    "text:(undefined) const [previewDragged, setPreviewDragged] = createSolidSignal",
    "text:(undefined) const [previewRenaming, setPreviewRenaming] = createSolidSignal",
    "text:: textProps.segment.italic ?",
    "text:: textProps.segment.text return textProps.segment.href === undefined ?",
    "text:B",
    "text:I",
    "text:byId.get(id)?.kind !== 'group'))) const renderedChildren = new Set",
    "text:childrenByKey().get(key)! return (",
    "text:containerWidth()",
    "text:controlProps.flow.row const liveRow = (): Extract",
    "text:disposeGridPointer?.()) const [renaming, setRenaming] = createSolidSignal",
    "text:gridGesture()?.id === id() return (",
    "text:gridItems().map(flowItemKey)) const fallbackItemsByKey = createMemo((): ReadonlyMap",
    "text:gridItemsByKey().get(key)! return",
    "text:groupProps.flow.children.map(flowItemKey)) return (",
    "text:item.grid === undefined && (arrange() || item.kind !== 'missing'))) const gridItemsByKey = createMemo((): ReadonlyMap",
    "text:item.kind === 'group') const addAutomaticPlacement = ( item: Extract",
    "text:mobileEditing() && id() !== undefined return (",
    "text:mobileItemsByKey().get(key)! return",
    "text:option.value === controlProps.row.value) const selectOptions = (): readonly ProductSelectOption",
    "text:row() as Extract",
    "text:selectProps.item.kind === 'control' || selectProps.item.kind === 'preview' || selectProps.item.kind === 'queue' || selectProps.item.kind === 'text' || selectProps.item.kind === 'missing' ? selectProps.item.parentId : undefined return (",
    "text:t.id === activeTabId()) // Same registry discipline as CanvasHost: a memo keyed on the reference, // so ws flaps (backendsTick churn) never rebuild rows. const registry = createMemo((): ReturnType",
    "text:target.graphId === option.graphId && target.nodeId === option.nodeId) return",
    "text:textProps.segment.bold ? textProps.segment.italic ?",
    "text:value + 1) const mobileItemKeys = createMemo",
    "text:void) | undefined const gridStyle = (grid: AppLayoutGrid): Record",
    "text:void) | undefined let presentationFocusTimer: ReturnType",
    "text:| (Omit"
  ],
  "AppWidgetPreview.tsx": [
    "text:observer?.disconnect()) return ("
  ],
  "AssetConsentDialog.tsx": [
    "text:decisionEl) const request = useSignal(props.app.assetConsent) const [selected, setSelected] = createSignal"
  ],
  "AssetEditorShell.tsx": [
    "text:Asset editor",
    "text:No editor tools were supplied.",
    "text:No fields were supplied.",
    "text:props.field.validation?.tone === 'warning' ? 'warning' as const : props.field.validation?.tone === 'info' || props.field.validation?.tone === 'status' ? 'status' as const : 'error' as const return ("
  ],
  "AssetsBody.tsx": [
    "text:() const [mounts, setMounts] = createSignal",
    "text:() const [sourceFailures, setSourceFailures] = createSignal",
    "text:([]) const [discoveryError, setDiscoveryError] = createSignal",
    "text:if (state.kind === 'error') return",
    "text:if (state.kind === 'no-compatible') return",
    "text:if (state.kind === 'no-match') return",
    "text:part.length",
    "text:return"
  ],
  "AudioRecorder.tsx": [
    "attribute:aria-label:Record audio",
    "text:('idle') const [message, setMessage] = createSignal(provider ? 'Microphone is off. Recording requires browser permission.' : 'Recording unavailable: this browser needs MediaRecorder and a secure microphone context.') let generation = 0 let stream: MediaStream | undefined let recorder: MediaRecorder | undefined let timer: ReturnType",
    "text:([]) const [device, setDevice] = createSignal('') const [limit, setLimit] = createSignal(60) const [recordedBytes, setRecordedBytes] = createSignal(0) const [state, setState] = createSignal",
    "text:Browser default",
    "text:Discard recording",
    "text:Find microphones",
    "text:Limit (s)",
    "text:Microphone",
    "text:Promise",
    "text:Record microphone",
    "text:Save recording",
    "text:Stop recording",
    "text:open(deviceId: string): Promise",
    "text:state() === 'idle' || state() === 'ready' return"
  ],
  "AudioTransport.tsx": [
    "attribute:alt:Server audio waveform",
    "attribute:aria-label:Next audio batch",
    "attribute:aria-label:Previous audio batch",
    "text:() const [waveform, setWaveform] = createSignal",
    "text:Audio waveform",
    "text:Batch selection not advertised by backend.",
    "text:Gain",
    "text:Loop",
    "text:Next",
    "text:Previous",
    "text:Window preview only. Loop and gain do not change the value."
  ],
  "BackendsPanel.tsx": [
    "text:supervisor() return ("
  ],
  "BadgePopover.tsx": [
    "text:Apply Replacement",
    "text:Definition missing.",
    "text:Dismiss",
    "text:No automatic replacement is available.",
    "text:No error details recorded.",
    "text:No log records for this run.",
    "text:No problem details recorded.",
    "text:Node missing.",
    "text:Open Subgraph",
    "text:Open in Execution log",
    "text:Open in Execution log",
    "text:ReplacementBadgeInfo"
  ],
  "BoundaryPanel.tsx": [
    "attribute:aria-label:Boundary editor",
    "attribute:aria-label:Maximum iterations",
    "attribute:aria-label:Region binding",
    "text:(undefined) const [nameDrafts, setNameDrafts] = createSolidSignal",
    "text:Binding",
    "text:Boundary",
    "text:Canvas destination",
    "text:Define what this workflow receives, returns, and exposes.",
    "text:Display name",
    "text:Exposure unavailable.",
    "text:Forward full slot",
    "text:Invalid stored selection.",
    "text:Maximum iterations",
    "text:Nested slots",
    "text:New nested slots are exposed automatically.",
    "text:Occurrence settings",
    "text:Only selected nested slots are exposed.",
    "text:Pin selection",
    "text:Pinned",
    "text:Read only",
    "text:Show",
    "text:The nested slot template cannot be resolved from the current schema catalog.",
    "text:This execution snapshot is frozen. You can inspect bindings and show their nodes on the canvas, but editing is unavailable.",
    "text:Track all slots",
    "text:Track all slots to reset this exposure safely.",
    "text:Tracks template",
    "text:Widget output",
    "text:Workflow interface",
    "text:autogrow",
    "text:continuation",
    "text:t.id === activeTabId()) // The ACTIVE tab's backend schema registry (same routing as CanvasHost). const registry = (): ReturnType"
  ],
  "CanvasHost.tsx": [
    "attribute:aria-label:Bookmarks. Show bookmark numbers on the minimap",
    "attribute:aria-label:Canvas view controls",
    "attribute:aria-label:Canvas widget controls",
    "attribute:aria-label:Close 3D orbit view",
    "attribute:aria-label:Dismiss refusal message",
    "attribute:aria-label:Fit graph to view (.)",
    "attribute:aria-label:Minimap controls",
    "attribute:aria-label:Minimap settings",
    "attribute:aria-label:Open 3D orbit view",
    "attribute:aria-label:Selected input previews",
    "attribute:aria-label:Zoom in (Alt+=)",
    "attribute:aria-label:Zoom out (Alt+-)",
    "attribute:data-tooltip-label:Fit graph to view",
    "attribute:data-tooltip-label:Minimap settings",
    "attribute:data-tooltip-label:Reset zoom to 100%",
    "attribute:data-tooltip-label:Show bookmark numbers on the minimap",
    "attribute:data-tooltip-label:Zoom in",
    "attribute:data-tooltip-label:Zoom out",
    "attribute:label:Download",
    "attribute:placeholder:Group title...",
    "attribute:placeholder:Node title...",
    "attribute:placeholder:Selector title...",
    "attribute:title:Embedded workflow found",
    "attribute:title:File could not be added",
    "text:() const [fileDropSurface, setFileDropSurface] = createSolidSignal",
    "text:() let gestureRefusalId = 0 let gestureRefusalTimer: ReturnType",
    "text:([]) const [activeCanvasMedia, setActiveCanvasMedia] = createSolidSignal",
    "text:([]) const [minimapMenu, setMinimapMenu] = createSolidSignal(false) const [minimapMenuPose, setMinimapMenuPose] = createSolidSignal",
    "text:([]) const [previewA11yTargets, setPreviewA11yTargets] = createSolidSignal",
    "text:([]) const [semanticScene, setSemanticScene] = createSolidSignal",
    "text:(new Map()) /** * Per-node run error slices from the same derivation: the one per-view * projection of the job error report that also drives the bottom error * badge, so badge, tooltip, and popover can never disagree. */ const [runErrorsByNode, setRunErrorsByNode] = createSolidSignal",
    "text:(new Map()) /** Node error badges the user dismissed, keyed by execution key (V9). */ const [dismissedErrorNodes, setDismissedErrorNodes] = createSolidSignal",
    "text:(new Map()) const [outputImagePreviews, setOutputImagePreviews] = createSolidSignal",
    "text:(new Set()) const [selectedNodeIds, setSelectedNodeIds] = createSolidSignal",
    "text:(setting)) return",
    "text:(settingsTick(), props.app.settings.get",
    "text:(settingsTick(), props.app.settings.get",
    "text:(undefined) /** * When the link menu opened. A midpoint single click opens it on release, * so the second press of a habitual double-click lands on the backdrop * milliseconds later; the backdrop ignores presses inside this window so * a double-click leaves the menu open instead of flashing it closed. */ let linkMenuOpenedAt = 0 const LINK_MENU_DISMISS_GRACE_MS = 350 const [recentNodeTypes, setRecentNodeTypes] = createSolidSignal",
    "text:(undefined) // Replacement scan items for the CURRENT graph, keyed by node id. Rebuilt // by the badges effect; read lazily by the deprecation badge popover. let replaceItems = new Map",
    "text:(undefined) const [linkDoubleClickMenu, setLinkDoubleClickMenu] = createSolidSignal",
    "text:, instancePath: readonly string[], ): ReadonlySet",
    "text:0) page(props.onPrevious) if (event.key === 'ArrowRight' && props.index",
    "text:3D viewer unavailable",
    "text:= new Set() // This instance's latest selection snapshot; mirrored into the // app-level canvasSelection signal only while this editor is focused. let instanceSelection: CanvasSelectionSnapshot = EMPTY_CANVAS_SELECTION let lastRerouteSelection: ReadonlySet",
    "text:= new Set() let hoverNode: string | undefined let dragOffsets: ReadonlyMap",
    "text:= node.x + region.x && worldX",
    "text:= node.y + region.y && worldY",
    "text:Add reroute",
    "text:Bookmarks",
    "text:Cancel",
    "text:Cancel",
    "text:Close",
    "text:Download",
    "text:Download",
    "text:Download",
    "text:FROZEN",
    "text:Follow Latest",
    "text:Go to Live",
    "text:Insert node...",
    "text:JSON.stringify([kind, node, port, members ?? []]) export const clipboardSceneEndpointTypes = (source: Scene): ReadonlyMap",
    "text:Load image",
    "text:Next",
    "text:Not resolved by this execution (branch not reached).",
    "text:Open embedded workflow",
    "text:Open image",
    "text:Orbit",
    "text:Pin Current Spec",
    "text:Previous",
    "text:Selector missing.",
    "text:This PNG contains a Comfy workflow. Choose one action; nothing runs automatically.",
    "text:This execution used this branch; the recorded choice never changes.",
    "text:Unpin Spec",
    "text:Value source missing.",
    "text:View Frozen",
    "text:[item.id, item]))) const [outputPages, setOutputPages] = createSolidSignal",
    "text:createRegistryPainter(widgetRegistry())) // Bumped whenever the active tab's document or graph stack changes; the // scene effect depends on it. Subscriptions rebind on tab switch. const [docTick, setDocTick] = createSolidSignal(0) const [widgetA11yTargets, setWidgetA11yTargets] = createSolidSignal",
    "text:document.querySelector",
    "text:element.dataset['outputKey'] === key) pager?.querySelector",
    "text:element.dataset['outputKey'] === props.outputKey) const nextControl = [...(replacement?.querySelectorAll",
    "text:entry['command'] === 'link.connect') if (connectIndex",
    "text:outputImagePreviewByKey().get(key) const [outputViewerKey, setOutputViewerKey] = createSolidSignal",
    "text:props.app.settings.get",
    "text:props.app.settings.get",
    "text:props.host?.focused() ?? true const executions = useSignal(props.app.store.executions) const overlayPins = useSignal(props.app.overlayPins) const lenses = useSignal(props.app.lenses) const backendsTick = useSignal(props.app.backendsTick) const tabTargets = useSignal(props.app.tabTargets) const settingsTick = useSignal(props.app.settings.changed) const collabTabs = useSignal(props.app.collabTabs) const modalPanel = useSignal(props.app.modalPanel) const diagnosticFocus = useSignal(props.app.diagnosticFocus) const problems = useSignal(props.app.problems) // Scene/widget diagnostics for THIS editor instance. The app-level // solveDiagnostics signal mirrors the focused instance only, so an // unfocused split sibling keeps its own rail without clobbering the // Problems panel. const [instanceDiagnostics, setInstanceDiagnostics] = createSolidSignal",
    "text:readonly endpointTypes: ReadonlyMap",
    "text:readonly key: string readonly pathKey: string readonly registry: ReturnType",
    "text:rect.right || client.y",
    "text:rect.right || clientY",
    "text:rect.right || e.clientY",
    "text:root-graph node ids (a subgraph instance is \"in // scope\" when ANY of its inner occurrences is). const rootIds = new Set",
    "text:schemas from this run are unavailable - nodes render without definitions",
    "text:undefined const schema: NodeSchema | undefined = entry.schema ?? resolve(entry.type) // Registry state can change after the palette opens. Preserve the old // unresolved-node fallback rather than turning that narrow race into a // silent no-op; only known schemas can contribute defaults or state. const values = schema ? (defaultValuesOf(schema) as Record"
  ],
  "CanvasSemanticNavigator.tsx": [
    "attribute:aria-label:Canvas scene navigator",
    "text:() const connections = new Map",
    "text:() const groupsForNode = new Map",
    "text:, pinNames: ReadonlyMap",
    "text:, reroutes: ReadonlyMap",
    "text:, selectors: ReadonlyMap",
    "text:, valueSources: ReadonlyMap",
    "text:= 0 && items[index]?.category === category) index -= 1 if (index",
    "text:Use arrow keys to browse. Enter selects and centers the item. Escape returns to the canvas.",
    "text:[selector.id, selector.title])) const pinNames = new Map",
    "text:pinConnectionKey( nodeId, pin.direction, pin.address.port, pin.address.members, boundary ? 'boundary' : pin.widgetTap === true ? 'widgetTap' : 'port', ) const pinName = (node: Pick",
    "text:readonly badges: Readonly"
  ],
  "CollabPanel.tsx": [
    "attribute:aria-label:Confirm ending session",
    "attribute:aria-label:Participants in this collaboration session",
    "text:!dismissedProposals().has(key)) const updateProposalState = ( setter: (value: (current: ReadonlySet",
    "text:('loading') const [confirmingEnd, setConfirmingEnd] = createSignal",
    "text:() let unsharedState: HTMLElement | undefined let currentSessionCard: HTMLElement | undefined let endButton: HTMLButtonElement | undefined let confirmEndButton: HTMLButtonElement | undefined let panel: HTMLDivElement | undefined let refreshRevision = 0 let refreshQueued = false const closedSessions = new Set",
    "text:([]) const [listState, setListState] = createSignal",
    "text:(new Map()) const [announcement, setAnnouncement] = createSignal('') const [sessions, setSessions] = createSignal",
    "text:(new Set()) const [dismissedProposals, setDismissedProposals] = createSignal",
    "text:(new Set()) const [failures, setFailures] = createSignal",
    "text:Active workflow",
    "text:Available now",
    "text:Backend",
    "text:Cancel",
    "text:Checking this backend for sessions you can join.",
    "text:Connect a Dinkster backend to discover and host shared sessions.",
    "text:Connect a Dinkster backend to host collaboration sessions.",
    "text:Current value",
    "text:Dismiss",
    "text:End for everyone",
    "text:Everyone will be disconnected. Their current workflow stays open locally.",
    "text:Invalid value",
    "text:Loading shared sessions",
    "text:No collaboration backend",
    "text:No shared sessions",
    "text:Not available in this client",
    "text:Not shared",
    "text:Participants",
    "text:Promise",
    "text:Proposals",
    "text:Proposed value",
    "text:ReadonlySet",
    "text:Refresh when someone else starts a session.",
    "text:Session",
    "text:Shared sessions",
    "text:This workflow",
    "text:You",
    "text:agent",
    "text:agent",
    "text:appliedProposals().has(key) return (",
    "text:confirmingEnd() === entry.descriptor.sessionId return (",
    "text:props.app.collabBackend() const [pending, setPending] = createSignal",
    "text:string | undefined, ): Promise"
  ],
  "CollectionPanel.tsx": [
    "text:( props.modeKey !== undefined ? readCollectionViewMode(storage, props.modeKey, props.defaultMode ?? 'list') : props.defaultMode ?? 'list', ) const [query, setQuery] = createSolidSignal('') const [folder, setFolder] = createSolidSignal('') const [filters, setFilters] = createSolidSignal",
    "text:([]) const [cursor, setCursor] = createSolidSignal",
    "text:(new Set()) const [armedAction, setArmedAction] = createSolidSignal",
    "text:(undefined) // Owner of the COMMITTED page (see onSourceAction). Invalidated the // moment a first-page refresh starts: the old items stay visible until // the replacement resolves, but page-scoped actions must not treat them // as current - the refresh may be re-paging against a different backend. const [pageOwner, setPageOwner] = createSolidSignal",
    "text:(undefined) const [expandedIds, setExpandedIds] = createSolidSignal",
    "text:(undefined) const [loading, setLoading] = createSolidSignal(false) const [error, setError] = createSolidSignal",
    "text:(undefined) const [selectedId, setSelectedId] = createSolidSignal",
    "text:(undefined) const [total, setTotal] = createSolidSignal",
    "text:JSX.Element /** Exact-match filter values applied before the first page loads. */ readonly initialFilters?: Readonly",
    "text:a.id === 'delete' && armedAction() === key() return",
    "text:e.id === row.dataset['entry']) if (index",
    "text:entry.id === selected) return index",
    "text:props.entryFallback?.(p.entry) ??",
    "text:props.searchPriority === 'primary' ?",
    "text:return",
    "text:return (",
    "text:s.id === sourceId()) const filterOptions = (filter: NonNullable",
    "text:viewportRect.bottom) viewport.scrollTop += entryRect.bottom - viewportRect.bottom else if (entryRect.top"
  ],
  "CompositorEditor.tsx": [
    "text:() const [error, setError] = createSignal",
    "text:() const [selectedId, setSelectedId] = createSignal",
    "text:layer.id === layerId) const to = from + direction if (to"
  ],
  "ConnectionProfiles.tsx": [
    "text:(listConnectionProfiles()) const [credentials, setCredentials] = createSignal",
    "text:(undefined) const [editedToken, setEditedToken] = createSignal('') const saveToken = async (profile: ConnectionProfile): Promise",
    "text:credentials().profiles.includes(id) // One credential mutation at a time in this window: the guard is set // synchronously before any await so the UI disables immediately. Other // windows on the same project have their own guard, so every mutation body // also runs under withConnectionProfilesLock, and token saves re-check the // profile still exists inside the lock. Without that, a save in one window // interleaved with a removal in another would re-store a credential whose // profile is being deleted, orphaning it. const [credentialBusy, setCredentialBusy] = createSignal(false) const add = async (): Promise"
  ],
  "ContextMenu.tsx": [
    "text:() const itemElements = new Map",
    "text:([]) const [indices, setIndices] = createSignal",
    "text:([firstEnabled(rootEntries())]) const [activeDepth, setActiveDepth] = createSignal(0) const [positions, setPositions] = createSignal",
    "text:= 0 && index",
    "text:menuFlat(props.menu.groups)) const [openPath, setOpenPath] = createSignal"
  ],
  "ContextPanel.tsx": [
    "text:case 'document': return",
    "text:case 'group': return",
    "text:case 'link': return",
    "text:case 'node': return",
    "text:case 'subgraph': return"
  ],
  "CurveEditor.tsx": [
    "attribute:aria-label:Interpolation",
    "text:() const [computed, setComputed] = createSignal",
    "text:() const [computedStatus, setComputedStatus] = createSignal('') const [selected, setSelected] = createSignal",
    "text:() const [error, setError] = createSignal",
    "text:Apply",
    "text:Cancel",
    "text:Curve editor",
    "text:Delete point",
    "text:Double-click the graph to add a point. Drag points to adjust them.",
    "text:Histogram from the selected run",
    "text:Interpolation",
    "text:Linear",
    "text:Monotone cubic",
    "text:No curve target is active.",
    "text:Value",
    "text:props.host?.tabId() ?? globalActiveTabId() const [documentTick, setDocumentTick] = createSignal(0) const [draft, setDraft] = createSignal"
  ],
  "CustomizeLayoutDialog.tsx": [
    "text:Choose which supported shell regions are visible.",
    "text:Reset visibility",
    "text:Visibility"
  ],
  "DesktopManagementDialog.tsx": [
    "text:() const [busy, setBusy] = createSignal(false) const [statusMessage, setStatusMessage] = createSignal",
    "text:() const [check, setCheck] = createSignal",
    "text:() const [mounts, setMounts] = createSignal",
    "text:() const [removingWorker, setRemovingWorker] = createSignal",
    "text:() const [workerName, setWorkerName] = createSignal('') const [workerEndpoint, setWorkerEndpoint] = createSignal('') const [workerTokenFile, setWorkerTokenFile] = createSignal('') const [workerTokenFileGrant, setWorkerTokenFileGrant] = createSignal",
    "text:() const [workerTlsCaFile, setWorkerTlsCaFile] = createSignal('') const [workerTlsCaFileGrant, setWorkerTlsCaFileGrant] = createSignal",
    "text:([]) const [mountPath, setMountPath] = createSignal('') const [mountId, setMountId] = createSignal('') const [workers, setWorkers] = createSignal",
    "text:([]) const [workerFormOpen, setWorkerFormOpen] = createSignal(false) const [editingWorker, setEditingWorker] = createSignal",
    "text:, successKey?: string): Promise",
    "text:= 1024 && unit",
    "text:Promise",
    "text:addMount(id: string, path: string, mode: 'read' | 'readwrite'): Promise"
  ],
  "DesktopSetup.tsx": [
    "text:D",
    "text:props.status.phase === 'failed' return ("
  ],
  "ExecutedImageViewer.tsx": [
    "attribute:aria-label:Compare split",
    "attribute:aria-label:Execution provenance",
    "attribute:aria-label:Fit image",
    "attribute:aria-label:Image viewer controls",
    "attribute:aria-label:Next image",
    "attribute:aria-label:Previous image",
    "attribute:aria-label:Selected output identity",
    "attribute:aria-label:Show all images",
    "attribute:aria-label:Zoom in",
    "attribute:aria-label:Zoom out",
    "attribute:title:Execution outputs",
    "text:('fit') const [failedKeys, setFailedKeys] = createSolidSignal",
    "text:(new Map()) const [loadedKeys, setLoadedKeys] = createSolidSignal",
    "text:(new Set()) const [naturalSizes, setNaturalSizes] = createSolidSignal",
    "text:Asset digest",
    "text:Availability",
    "text:Backend",
    "text:Backend ID",
    "text:Batch index",
    "text:Compare slider",
    "text:Descriptor",
    "text:Execution",
    "text:Execution owner removed or disconnected. Retained output routes are used when available.",
    "text:File type",
    "text:Fit",
    "text:Grid",
    "text:Image",
    "text:Image",
    "text:List path",
    "text:Loading image...",
    "text:Media kind",
    "text:Media type",
    "text:Name",
    "text:Next",
    "text:Next",
    "text:Next grid page",
    "text:No execution images are available.",
    "text:Output ID",
    "text:Previous",
    "text:Previous",
    "text:Previous grid page",
    "text:Resolution",
    "text:Runtime node",
    "text:Side by side",
    "text:Single",
    "text:Source",
    "text:Status",
    "text:Subfolder",
    "text:Type",
    "text:Unavailable",
    "text:props.batch?.imageAt(index) ?? props.images[index] const [index, setIndex] = createSolidSignal(clamp(props.initialIndex, count())) const [rightIndex, setRightIndex] = createSolidSignal(clamp(props.initialIndex + 1, count())) const [mode, setMode] = createSolidSignal",
    "text:side.name === 'Left' ? setIndex(clamp(next, count())) : setRightIndex(clamp(next, count())) return"
  ],
  "ExecutionActivityCard.tsx": [
    "text:Following latest",
    "text:Pinned to canvas",
    "text:Snapshot unavailable - submitted by another client",
    "text:agent",
    "text:state === 'done' || state === 'cached' const scheduledGraphNodes = ( entry: ExecutionState, graph: DinksterGraphWire, prefix = '', ): ReadonlySet"
  ],
  "ExecutionLogPanel.tsx": [
    "text:(new Set()) // '' = all nodes; a runtime node id filters to that node's rows. const [nodeFilter, setNodeFilter] = createSignal('') // Badge-driven grouped filter: one scene node's runtime occurrences. Any // manual node selection clears it. const [groupFilter, setGroupFilter] = createSignal",
    "text:const id = row.runtimeNodeId return ("
  ],
  "GlslEditor.tsx": [
    "attribute:aria-label:Fragment shader source",
    "attribute:aria-label:GLSL browser preview",
    "attribute:aria-label:Preview output",
    "attribute:aria-label:Shader inputs",
    "text:() const [applyError, setApplyError] = createSignal",
    "text:(0) const [images, setImages] = createSignal",
    "text:256 samples",
    "text:Apply",
    "text:Browser preview uses bounded run inputs. Native execution is authoritative.",
    "text:Cancel",
    "text:Fragment shader",
    "text:GLSL Shader",
    "text:No GLSL Shader target is active.",
    "text:No runtime state.",
    "text:Preview output",
    "text:Run inputs",
    "text:This input is linked or no longer available. Disconnect it to edit.",
    "text:fragColor0",
    "text:fragColor1",
    "text:fragColor2",
    "text:fragColor3",
    "text:image",
    "text:props.host?.tabId() ?? globalActiveTabId() const [documentTick, setDocumentTick] = createSignal(0) const [draft, setDraft] = createSignal('') const [output, setOutput] = createSignal"
  ],
  "ImageDocumentWorkspace.tsx": [
    "text:() const [authoritativeBusy, setAuthoritativeBusy] = createSignal(false) const [authoritativeResult, setAuthoritativeResult] = createSignal",
    "text:() const [authoritativeError, setAuthoritativeError] = createSignal",
    "text:() const [libraryOpen, setLibraryOpen] = createSignal(false) const [libraryRecords, setLibraryRecords] = createSignal",
    "text:([]) const [activeLineage, setActiveLineage] = createSignal('') const [loading, setLoading] = createSignal(true) const [busy, setBusySignal] = createSignal(false) const [error, setErrorValue] = createSignal",
    "text:([]) const [collaborationOpen, setCollaborationOpen] = createSignal(false) const [collaborationSessions, setCollaborationSessions] = createSignal",
    "text:([]) const [confirmingEnd, setConfirmingEnd] = createSignal(false) const [collaborationTransition, setCollaborationTransition] = createSignal(false) const [resourceTick, setResourceTick] = createSignal(0) const [metadataTick, setMetadataTick] = createSignal(0) let importInput!: HTMLInputElement let tablist!: HTMLDivElement let libraryDialog!: HTMLDivElement let collaborationDialog!: HTMLDivElement let endConfirmButton: HTMLButtonElement | undefined let live = true const leaving = new Set",
    "text:Promise",
    "text:Promise",
    "text:Promise",
    "text:Promise",
    "text:Promise",
    "text:Promise",
    "text:candidate.layer.id === layerId)! return (",
    "text:documentValue().masks[maskId]! return (",
    "text:hydrate: Promise",
    "text:tablist.querySelector",
    "text:void name: string draft: ImageDocumentDraft persist: Promise",
    "text:window.removeEventListener('keydown', onShortcut)) const updateLayer = (changes: Omit"
  ],
  "ImageEditor.tsx": [
    "attribute:aria-label:Mask tools",
    "text:() const [error, setError] = createSignal",
    "text:= 0.5 && point.x - radius",
    "text:= 0.5 && point.y - radius",
    "text:Cancel",
    "text:Clear",
    "text:Erase",
    "text:Hardness",
    "text:Image editor",
    "text:Invert",
    "text:Loading source image...",
    "text:Mask",
    "text:No image target is active.",
    "text:No image target is active.",
    "text:Paint",
    "text:Redo mask edit",
    "text:Size",
    "text:Undo mask edit",
    "text:isCompositorEditorTarget(request()) ?",
    "text:props.host?.tabId() ?? globalActiveTabId() const [session, setSession] = createSignal"
  ],
  "ImportAssetResolutionDialog.tsx": [
    "text:decisionEl) const request = useSignal(props.app.importAssetResolution) const [selected, setSelected] = createSignal"
  ],
  "LibraryPanel.tsx": [
    "text:() const settleTimers = new Set",
    "text:: activitySource() ?",
    "text:: activitySource() ?",
    "text:: directSource() || activitySource() ?",
    "text:controls?.querySelectorAll",
    "text:directSource() ?",
    "text:transitioning() ?"
  ],
  "MemoryPanel.tsx": [
    "attribute:aria-label:Active memory leases",
    "attribute:aria-label:Memory history table",
    "attribute:aria-label:Page flag ranges table",
    "text:('idle') const [detailReady, setDetailReady] = createSignal",
    "text:() const [detailState, setDetailState] = createSignal",
    "text:() const [details, setDetails] = createSignal",
    "text:(new Set()) const [baseError, setBaseError] = createSignal('') const [detailError, setDetailError] = createSignal('') const [connectionState, setConnectionState] = createSignal(props.connection.status.get()) const [receivedAt, setReceivedAt] = createSignal(0) const [expanded, setExpanded] = createSignal",
    "text:(new Set()) const [clock, setClock] = createSignal(props.now?.() ?? Date.now()) const [history, setHistory] = createSignal",
    "text:= 1024 && unit",
    "text:Active leases",
    "text:Aimdo-corrected free",
    "text:Available",
    "text:Backend",
    "text:Backend disconnected. All visible telemetry is retained from the last update.",
    "text:Budget",
    "text:Budget and headroom changes apply live. Aimdo policy applies to workers started after the change.",
    "text:Capacity",
    "text:Consumers",
    "text:Detail telemetry is unsupported for this consumer.",
    "text:Device",
    "text:Footprint",
    "text:Footprint",
    "text:Governor metrics are unavailable for this device. Execution occupancy is still authoritative.",
    "text:Lease telemetry is unsupported on this backend.",
    "text:Loading item details...",
    "text:Memory and Aimdo controls",
    "text:Memory governor telemetry is unsupported on this backend. Execution occupancy remains available.",
    "text:No active leases",
    "text:No consumers",
    "text:No devices reported",
    "text:No item details",
    "text:No temporary memory reservations are reported.",
    "text:Pages",
    "text:Queue state",
    "text:Queue, device, lease, and consumer facts will appear when the backend responds.",
    "text:Queued",
    "text:Raw capacity",
    "text:Reservations",
    "text:Reserved",
    "text:Reserved",
    "text:Residency",
    "text:Resident",
    "text:Retained values remain visible.",
    "text:Running",
    "text:Server flag",
    "text:Server settings",
    "text:Telemetry is stale. Retained values remain visible while polling retries.",
    "text:The backend returned no execution or memory device facts.",
    "text:The backend supports details but returned no items for this consumer.",
    "text:This device currently reports no resident memory consumers.",
    "text:Time",
    "text:Transition",
    "text:Waiting for memory telemetry",
    "text:props.backendId ?? props.label let clockTimer: ReturnType",
    "text:pulse === 'in' ? 'Moved in' : pulse === 'out' ? 'Moved out' : 'No change' return",
    "text:type Timer = ReturnType",
    "text:| undefined const initializedCollapseKeys = new Set"
  ],
  "ModalSurface.tsx": [
    "text:rect.right || event.clientY",
    "text:void readonly onKeyDown?: JSX.EventHandler"
  ],
  "Model3dPosterImage.tsx": [
    "attribute:alt:3D model preview",
    "text:(poster.state === 'ready' ? poster() : undefined) return (",
    "text:3D preview unavailable.",
    "text:Download model",
    "text:Rendering 3D preview..."
  ],
  "NodePalette.tsx": [
    "text:() const duplicates = new Set",
    "text:(new Set(props.anchor.initialInputTypes)) const [paletteOutputTypes, setPaletteOutputTypes] = createSolidSignal",
    "text:(new Set(props.anchor.initialOutputTypes)) const [expandedCategories, setExpandedCategories] = createSolidSignal",
    "text:(undefined) const [paletteInputTypes, setPaletteInputTypes] = createSolidSignal",
    "text:0) if (focusable.length === 0) return const index = focusable.indexOf(document.activeElement as HTMLElement) if (event.shiftKey && index",
    "text:duplicatePaletteNames(paletteEntries())) const [paletteQuery, setPaletteQuery] = createSolidSignal('') const [paletteIndex, setPaletteIndex] = createSolidSignal(0) const [paletteCategory, setPaletteCategory] = createSolidSignal",
    "text:entry().schema?.description ?? entry().blueprint?.descriptor.description return (",
    "text:onChange: (selected: ReadonlySet",
    "text:paletteEntryIdentity(entry, duplicateNames()) return (",
    "text:paletteResize?.disconnect()) return (",
    "text:paletteResults?.querySelector",
    "text:panel ? Array.from(panel.querySelectorAll"
  ],
  "OutputDescriptorEditor.tsx": [
    "text:() const [busy, setBusy] = createSignal(false) const [raw, setRaw] = createSignal(false) const [probeCurrent, setProbeCurrent] = createSignal"
  ],
  "P2PPanel.tsx": [
    "text:() const [draft, setDraft] = createSignal",
    "text:() const [loading, setLoading] = createSignal(true) const [busy, setBusy] = createSignal(false) const [notice, setNotice] = createSignal('') const [error, setError] = createSignal('') const [invalid, setInvalid] = createSignal",
    "text:() const [status, setStatus] = createSignal",
    "text:(new Set",
    "text:= 0 && (key === 'internetSeedRatio' || (Number.isSafeInteger(scaled) && scaled",
    "text:invalid().has(key) return"
  ],
  "ProductControls.tsx": [
    "text:('[role=\"radio\"]')] const current = radios.indexOf(control) for (let offset = 1; offset",
    "text:void readonly id?: string readonly class?: string readonly testId?: string readonly ariaLabel?: string readonly ariaLabelledBy?: string readonly ariaDescribedBy?: string | undefined readonly ariaInvalid?: boolean readonly disabled?: boolean readonly enterActivates?: boolean readonly dataAttributes?: Readonly"
  ],
  "ProductForm.tsx": [
    "text:props.messageTone ?? 'error' return (",
    "text:props.tone === 'error' || props.tone === 'warning' ? 'alert' : props.tone === 'status' ? 'status' : undefined return ("
  ],
  "ProductNumberInput.tsx": [
    "attribute:aria-label:Decrease value",
    "attribute:aria-label:Increase value",
    "text:= BigInt(Number.MIN_SAFE_INTEGER) && parsed",
    "text:props.integer === true ? safeInteger(raw()) : valid(raw()) ? Number(raw()) : undefined return ("
  ],
  "ProductSelect.tsx": [
    "text:[] readonly selectedId: string readonly onSelect: (option: ProductSelectOption",
    "text:option.id === props.selectedId) const selectedOption = (): ProductSelectOption",
    "text:readonly onFocus?: JSX.EventHandlerUnion",
    "text:readonly onPointerDown?: JSX.EventHandlerUnion",
    "text:void readonly id?: string readonly class?: string readonly testId?: string readonly ariaLabel?: string readonly ariaLabelledBy?: string readonly ariaDescribedBy?: string | undefined readonly ariaInvalid?: boolean readonly disabled?: boolean readonly dataAttributes?: Readonly"
  ],
  "ProductSlider.tsx": [
    "text:props.min ? ((value() - props.min) / (props.max - props.min)) * 100 : 0 return ("
  ],
  "ProductTabs.tsx": [
    "text:) return (",
    "text:tab.id === fromId) const next = direction === 'first' ? available[0] : direction === 'last' ? available.at(-1) : available[(current + direction + available.length) % available.length] tablist.querySelector"
  ],
  "ProjectsDialog.tsx": [
    "attribute:aria-label:New project name",
    "attribute:placeholder:New project name",
    "text:A project keeps its own tabs, backend connections, and layout. Windows on the same project stay in sync.",
    "text:Create project",
    "text:Current",
    "text:New window",
    "text:Rename",
    "text:Switch"
  ],
  "RuntimeSettingsPanel.tsx": [
    "text:!sameValue(submittedValue(), props.section.value) const save = async (event: SubmitEvent): Promise",
    "text:() const [saving, setSaving] = createSignal(false) const [budgetMaximums, setBudgetMaximums] = createSignal",
    "text:([]) const [comfyArgRows, setComfyArgRows] = createSignal",
    "text:([]) const [error, setError] = createSignal('') const [rejected, setRejected] = createSignal",
    "text:(props.section.value) const [loggingRows, setLoggingRows] = createSignal",
    "text:Math.round(sizeBytes(value) / 1024 ** 2) const CATEGORY_LABELS: Readonly",
    "text:const asRecord = (value: unknown): Record",
    "text:settings().settings[category] return",
    "text:typeof value === 'object' && value !== null && !Array.isArray(value) ? value as Record"
  ],
  "SearchSurface.tsx": [
    "text:props.kind === 'loading' ? LoaderCircle : props.kind === 'error' ? CircleAlert : SearchX return (",
    "text:readonly onKeyDown: JSX.EventHandler",
    "text:void) | undefined readonly onInput: JSX.EventHandler"
  ],
  "SettingsDialog.tsx": [
    "text:() const [capture, setCapture] = createSignal",
    "text:() const [error, setError] = createSignal",
    "text:, ): Promise",
    "text:capture()?.command === command.id ? capture()?.pending || message('settingsDialog.keybinding.pressKeys') : combo() ?? message('settingsDialog.keybinding.unbound') const ids = productFieldIds(controlId) return",
    "text:props.settings.categoryOf(d))) const [searchCategory, setSearchCategory] = createSignal",
    "text:updatePackSettings( packId: string, values: Readonly"
  ],
  "ShellChrome.tsx": [
    "text:('.dock-zone-actions') const actionGap = actions === null ? 0 : Number.parseFloat(getComputedStyle(actions).columnGap) || 0 const overflowWidth = overflowRoot !== undefined && root.contains(overflowRoot) ? overflowRoot.getBoundingClientRect().width + actionGap : 0 const widthWithoutOverflow = tablist.clientWidth + overflowWidth const allFloorsWidth = count * floor + Math.max(0, count - 1) * gap const visibleCount = allFloorsWidth",
    "text:('[role=\"tablist\"]') const tab = tablist?.querySelector",
    "text:(new Set()) // Tab entries cache per descriptor so a registry tick re-maps the array // without recreating entries, keeping panel bodies mounted across updates. // Each entry lives in its own root parented to the section (not to the // reactive computation that happened to first render it), so a body // created after a panel returns from floating/window placement is not // disposed - and its reactivity frozen - by the next tab-list update. // Eviction disposes the root so a departed body releases its // subscriptions instead of leaking. const owner = getOwner() const entries = new Map",
    "text:0) setHiddenTabIds(new Set",
    "text:disposeDrag?.()) // Placement moves (dock, new window) live in the header's context menu; // Dock stays as the one always-visible chrome action. const [placementMenu, setPlacementMenu] = createSignal",
    "text:props.sections[0]?.section // Placement moves (float, new window) for one tab's panel, offered through // the shared styled context menu instead of always-visible chrome buttons. const [placementMenu, setPlacementMenu] = createSignal"
  ],
  "SubgraphDefinitionsDialog.tsx": [
    "text:Body nodes",
    "text:Every subgraph definition is reachable from the workflow root.",
    "text:No editable workflow",
    "text:No reusable subgraph definitions are reachable from the workflow root.",
    "text:No unused definitions",
    "text:None",
    "text:Occurrences",
    "text:Open an editable workflow to review its subgraph definitions.",
    "text:Related saved items",
    "text:These definitions cannot be reached from the workflow root. Removing them also removes their saved layout and core-owned bookmarks, app controls, net positions, and mode-panel bindings.",
    "text:Unused definitions",
    "text:entry.id === id) return"
  ],
  "SuggestionSurface.tsx": [
    "text:(): SuggestionInteraction",
    "text:readonly interaction: SuggestionInteraction"
  ],
  "TimelineViewport.tsx": [
    "attribute:aria-label:Playhead seconds",
    "attribute:aria-label:Timeline playhead",
    "attribute:aria-label:Timeline viewport",
    "attribute:aria-label:Timeline zoom",
    "attribute:aria-label:Timeline zoom level",
    "attribute:aria-label:Zoom in timeline",
    "attribute:aria-label:Zoom out timeline",
    "text:= 0 && left + timelineTimeLabel(time).length * 8 + 6",
    "text:Fit timeline",
    "text:Playhead (seconds)",
    "text:Time (seconds)",
    "text:Timeline"
  ],
  "UniversalSearch.tsx": [
    "attribute:aria-label:Search Dinkster",
    "attribute:aria-label:Search results",
    "attribute:placeholder:Search commands, nodes, settings, and tabs",
    "attribute:title:Provider unavailable",
    "text:&gt;",
    "text:(); const [expanded, setExpanded] = createSignal",
    "text:([]) const [selected, setSelected] = createSignal",
    "text:Commands",
    "text:Nodes",
    "text:Search Dinkster",
    "text:Settings",
    "text:Tabs",
    "text:Up/Down: Navigate | Enter: Open | Esc: Close",
    "text:keyOf(row) === key()) return (",
    "text:keyOf(row) === selected()) return found",
    "text:results?.querySelector",
    "text:visible()[selectedIndex()] === undefined ? undefined : optionId(selectedIndex()) const activeResult = (): Extract"
  ],
  "VideoDocumentEditor.tsx": [
    "text:(error() === undefined ? undefined : '$raw') const setObject = (next: Record",
    "text:(initialParams) const [drafts, setDrafts] = createSignal",
    "text:(parsed === undefined && props.command !== 'import_otio' ? message('videoDocument.error.object') : undefined) const [errorField, setErrorField] = createSignal",
    "text:[field.key, fieldText(field, initialParams)]), )) const [raw, setRaw] = createSignal(props.command === 'import_otio' || parsed === undefined ? props.value : JSON.stringify(parsed, null, 2)) const [error, setError] = createSignal"
  ],
  "VideoEditEditor.tsx": [
    "attribute:alt:Crop source frame",
    "attribute:alt:Selected source frame",
    "attribute:alt:Source thumbnails",
    "attribute:aria-label:Bounded source preview",
    "attribute:aria-label:Crop ratio presets",
    "attribute:aria-label:Crop video",
    "attribute:aria-label:Playhead frame",
    "attribute:aria-label:Trim end handle",
    "attribute:aria-label:Trim start handle",
    "attribute:aria-label:Trim video",
    "text:() const [numericErrors, setNumericErrors] = createSignal",
    "text:() const [playPreview, setPlayPreview] = createSignal(false) const [decodeError, setDecodeError] = createSignal",
    "text:(initial) const [strictDuration, setStrictDuration] = createSignal(props.strictDuration ?? false) const [playhead, setPlayhead] = createSignal(0) const [peek, setPeek] = createSignal",
    "text:Apply",
    "text:Cancel",
    "text:Crop",
    "text:Draft controls overlay the source preview. Apply and run the node for the rendered trim/crop result.",
    "text:Duration seconds",
    "text:End frame",
    "text:Filmstrip unavailable",
    "text:Frame selection is not advertised. Only the provider's default source frame or poster can be shown.",
    "text:Full frame",
    "text:One clip - seconds stored, frames shown",
    "text:Source pixels - chroma-even",
    "text:Source timing or dimensions are unavailable. Trim handles require timing; crop handles require dimensions. Numeric fields remain editable.",
    "text:Start frame",
    "text:Start seconds",
    "text:Strict duration",
    "text:Trim",
    "text:updateVideoEditSection(value, 'trim', fields)) const updateCrop = (fields: Readonly"
  ],
  "VideoPreview.tsx": [
    "attribute:alt:Video poster",
    "attribute:aria-label:Next frame",
    "attribute:aria-label:Next video",
    "attribute:aria-label:Previous frame",
    "attribute:aria-label:Previous video",
    "attribute:aria-label:Video outputs",
    "attribute:aria-label:Video poster",
    "attribute:aria-label:Video preview",
    "attribute:aria-label:Video preview controls",
    "attribute:title:Next frame (.)",
    "attribute:title:Play preview from its playback time, not the indexed inspection frame",
    "attribute:title:Previous frame (,)",
    "attribute:title:Reported FPS; not used to convert time and frame index",
    "text:() const [hovering, setHovering] = createSignal(false) const [error, setError] = createSignal('') const [color, setColor] = createSignal",
    "text:() const [indexedFrame, setIndexedFrame] = createSignal",
    "text:() const [mime, setMime] = createSignal",
    "text:() const [playbackColor, setPlaybackColor] = createSignal",
    "text:() const [position, setPosition] = createSignal(0) const [duration, setDuration] = createSignal(0) const [playing, setPlaying] = createSignal(false) const [inspecting, setInspecting] = createSignal(false) const [frame, setFrame] = createSignal",
    "text:() const [src, setSrc] = createSignal",
    "text:() const [thumbColor, setThumbColor] = createSignal",
    "text:() const [thumbs, setThumbs] = createSignal",
    "text:Download preview",
    "text:Loading video...",
    "text:Next",
    "text:Previous",
    "text:props.preferences ?? localPreferences() const [inspection, setInspection] = createSignal"
  ],
  "WidgetEditor.tsx": [
    "text:return"
  ],
  "WidgetEditorController.tsx": [
    "attribute:aria-label:Edit output descriptors",
    "text:) : ed.outputDescriptors !== undefined ? (",
    "text:) : null return (",
    "text:) : props.implementation !== undefined ? (",
    "text:videoDocumentCommand !== undefined ? (",
    "text:void editor: WidgetEditorProps header: Component"
  ],
  "WorkflowQueueControl.tsx": [
    "attribute:aria-label:Workflow jobs",
    "attribute:data-tooltip-detail:Ctrl+Enter",
    "attribute:data-tooltip-label:Queue workflow",
    "text:No jobs for this workflow.",
    "text:Queue"
  ],
  "WorkflowTabs.tsx": [
    "text:(props: WorkflowTabsProps"
  ],
  "asset-browser/presentation.tsx": [
    "text:family() === 'folder' ? Folder : family() === 'image' ? ImageIcon : family() === 'video' ? Film : family() === 'audio' ? AudioLines : family() === 'model' || family() === 'model3d' ? Box : File return ("
  ],
  "asset-browser/rail.tsx": [
    "text:() const [previewFailed, setPreviewFailed] = createSignal(false) return (",
    "text:adapter.id === props.item.source.id) return (",
    "text:props.detail.variant return (",
    "text:props.entry?.thumbUrl ?? (props.assetUrl !== undefined && selected.digest !== undefined && isBrowserPreviewable(selected) ? props.assetUrl(selected.digest) : undefined) return (",
    "text:void) | undefined readonly additionalFacts?: NonNullable"
  ],
  "editors/AssetWidgetEditor.tsx": [
    "attribute:alt:Asset preview",
    "attribute:aria-label:Asset requirements",
    "attribute:aria-label:Download provider",
    "attribute:aria-label:Missing model resolution",
    "attribute:aria-label:Selection",
    "attribute:title:Loading models",
    "attribute:title:No exact local matches",
    "text:([]) const [assetMatchPending, setAssetMatchPending] = createSolidSignal(false) const [assetMatchError, setAssetMatchError] = createSolidSignal",
    "text:(undefined) /** The browsed item behind the staged selection, when the pick came from * the collection browser: feeds the Details disclosure. Uploads and * reopened stored values have only an AssetRef, so this stays unset. */ const [assetSelectedItem, setAssetSelectedItem] = createSolidSignal",
    "text:(undefined) /** The model variant behind the staged selection, when the pick came * from the logical model picker: feeds the Details disclosure. */ const [assetSelectedModel, setAssetSelectedModel] = createSolidSignal",
    "text:(undefined) const [assetError, setAssetError] = createSolidSignal",
    "text:(undefined) const [assetPreviewFailed, setAssetPreviewFailed] = createSolidSignal(false) /** Natural pixel size of the previewed media, measured on load; feeds the * caption under the preview. */ const [assetPreviewDims, setAssetPreviewDims] = createSolidSignal",
    "text:(undefined) const [assetSourcesError, setAssetSourcesError] = createSolidSignal",
    "text:(undefined) const [logicalModels, setLogicalModels] = createSolidSignal",
    "text:(undefined) const [logicalModelsError, setLogicalModelsError] = createSolidSignal",
    "text:(undefined) const [logicalModelsLoading, setLogicalModelsLoading] = createSolidSignal(false) const unresolvedAsset = typeof ed.initial === 'string' ? ed.initial : undefined const [assetMatchCandidates, setAssetMatchCandidates] = createSolidSignal",
    "text:/ list",
    "text:Accepted types",
    "text:Browse",
    "text:Cancel",
    "text:Choose a local replacement",
    "text:Download",
    "text:Download unavailable",
    "text:Download video",
    "text:Download video",
    "text:Expected kind",
    "text:File name",
    "text:Image preview unavailable.",
    "text:Open or download image",
    "text:Refresh",
    "text:Remove",
    "text:Requested",
    "text:Selection",
    "text:The imported reference is not available. Select an exact local asset below.",
    "text:This backend provides no download source or acquisition contract.",
    "text:Unresolved import",
    "text:Video preview unavailable.",
    "text:assetBrowseKind()?.startsWith('model/') === true ? MODEL_ASSET_VOCABULARY : assetBrowseKind() === 'data/latent' ? LATENT_ASSET_VOCABULARY : GENERIC_ASSET_VOCABULARY // Multi-select (typed-assets pin (5)): the declared outer type gates it - // list",
    "text:assetUploadAbort?.abort()) // undefined = discovery not settled; [] = settled with nothing browsable. const [assetSources, setAssetSources] = createSolidSignal",
    "text:close() const Implementation: Component",
    "text:if (state.kind === 'error') return",
    "text:if (state.kind === 'no-compatible') return",
    "text:if (state.kind === 'no-match') return",
    "text:logicalModelAbort?.abort()) const loadLogicalModels = async (sources: readonly AssetSourceAdapter[]): Promise",
    "text:pick N descriptors committed as ONE // array value, and a scalar concrete T with a registered batch-merge // provider (mergeableTypes membership) picks N descriptors the engine // decodes-each-then-merges into ONE batched T. Every other declaration // stays single-select: a pick stages one descriptor and the footer Apply // commits it. asset",
    "text:requestClose(close), help:",
    "text:return",
    "text:stays single-select (decoder multi-ness). const assetMulti = ed.spec?.widgetType === 'ASSET' && ed.declaredType !== undefined && assetMultiSelect(ed.declaredType, ed.mergeableTypes) const sourceUpload = ed.spec?.widgetType === 'ASSET' && ed.spec.allowUpload === true && ed.sourceFilename !== undefined const uploadKind = ed.sourceFilename?.kind ?? assetBrowseKind() const classifiedMediaKind = ed.spec?.widgetType === 'ASSET' && (uploadKind === 'media/image' || uploadKind === 'media/audio' || uploadKind === 'media/video') ? uploadKind : undefined const classifiedLatent = ed.spec?.widgetType === 'ASSET' && uploadKind === 'data/latent' const assetUploadMaxBytes = classifiedMediaKind === 'media/image' ? IMAGE_UPLOAD_MAX_BYTES : classifiedMediaKind !== undefined ? MEDIA_UPLOAD_MAX_BYTES : classifiedLatent ? LATENT_UPLOAD_MAX_BYTES : GENERIC_UPLOAD_MAX_BYTES // Display copy exists only for kinds with a deliberate, user-facing limit. // Model kinds fall into the generic enforcement bucket, whose 16 MiB bound // is not an authoritative model limit, so they render no limit copy // (enforcement still applies through assetUploadMaxBytes). const assetUploadLimitLabel = classifiedMediaKind === 'media/image' ? '256 MiB per file' : classifiedMediaKind !== undefined || classifiedLatent ? '1 GiB per file' : uploadKind?.startsWith('model/') === true ? undefined : '16 MiB per file' // The SCALAR MERGE arm specifically (multi-select whose declaration is // NOT list-outer): an empty selection cannot commit `[]` there - the // declared type is scalar T, and a bare empty array would compile as raw // junk the merge provider rejects (zero batches). Applying empty in this // mode commits null (unset), mirroring the scalar clear semantics; // list-outer declarations keep the pinned `[]` commit. const assetScalarMerge = assetMulti && ed.declaredType !== undefined && !assetMultiSelect(ed.declaredType) const [assetSelection, setAssetSelection] = createSolidSignal"
  ],
  "editors/ColorWidgetEditor.tsx": [
    "attribute:label:Color value",
    "text:Cancel",
    "text:Commit",
    "text:props.editor.bindClickAway?.(undefined)) return"
  ],
  "editors/ComboEditorImplementation.tsx": [
    "attribute:aria-label:Back to parent folder",
    "attribute:label:Search options",
    "text:([]) const [values, setValues] = createSignal",
    "text:= previous.start && highlighted",
    "text:Apply",
    "text:Back",
    "text:Cancel",
    "text:Clear",
    "text:No matching options",
    "text:No selections",
    "text:comboMenuEntries(availableOptions(), folder(), query())) const window = createMemo",
    "text:loading remote options...",
    "text:memberIds[String(option.value)] !== undefined) : familyOptions const [labels, setLabels] = createSignal",
    "text:option.value === value) ? undefined : spec.remote ? 'warning' : 'error' return",
    "text:refresh",
    "text:remote options stale",
    "text:remote options unavailable"
  ],
  "editors/SaveTargetWidgetEditor.tsx": [
    "attribute:aria-label:Save target output metadata",
    "attribute:label:Mount",
    "attribute:label:Prefix",
    "text:Output format",
    "text:cancel",
    "text:clear",
    "text:loading mounts...",
    "text:mount list failed to load",
    "text:no writable mounts available",
    "text:props.editor.bindClickAway?.(undefined)) return",
    "text:save"
  ],
  "editors/ScalarWidgetEditor.tsx": [
    "attribute:aria-label:Numeric constraints",
    "attribute:label:Value",
    "text:) | (CompletionContext & Omit",
    "text:Cancel",
    "text:Commit",
    "text:Maximum",
    "text:Minimum",
    "text:Step",
    "text:host.viewport().scale return",
    "text:session().status !== 'idle' const surfaceState = createMemo",
    "text:session().status === 'ready' ? (session() as Extract"
  ],
  "help/LearnPanel.tsx": [
    "text:() const [guideState, setGuideState] = createSignal",
    "text:selectedLocale(descriptor) return ("
  ],
  "host-ui.tsx": [
    "text:() private readonly changedSignal = createSignal(0) private batchDepth = 0 private batchDirty = false get changed(): ReadonlySignal"
  ],
  "tabs-proof.tsx": [
    "text:Activity content",
    "text:Disabled content",
    "text:Nearby content",
    "text:Outside focus target",
    "text:Overview content",
    "text:Product tabs proof"
  ]
}
