{
  "_provenance": "Derived once from the dinkster-serve dev-pack catalog (packages/dinkster-nodes-dev/dinkster-pack.toml) at Dinkster 8465b152, wire /api/nodes, 2026-09-24. Refreshing this file is a reviewed diff; tests must never read the live catalog to build expectations.",
  "schemaCount": 507,
  "emptyLatent": {
    "type": "dinkster.empty_latent_image",
    "alias": "EmptyLatentImage",
    "inputs": ["width", "height", "batch_size"],
    "outputs": ["latent"]
  },
  "emptySd3Latent": {
    "type": "dinkster.empty_sd3_latent_image",
    "inputs": ["width", "height", "batch_size"],
    "outputs": ["latent"]
  },
  "vaeDecode": {
    "type": "dinkster.vae_decode",
    "inputs": ["samples", "vae"],
    "outputs": ["image"]
  }
}
